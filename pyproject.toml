[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "artificial-scientist-experiments"
version = "0.1.0"
description = "Small reproducible experiments in prediction and adaptation"
requires-python = ">=3.9"
dependencies = []
license = {file = "LICENSE"}

[tool.setuptools.packages.find]
include = ["artificial_scientist*"]
