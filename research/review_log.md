# Review log

The complete [historical review log](https://github.com/marcomontagna/artificial-scientist/blob/0c7c3e4dbe3405a0f681b31fbc92e47aefac9ad9/research/review_log.md) remains in Git history. Its test counts and approvals describe retired implementations, not the current implementation.

## Direction reset — September 26, 2026

Author: Codex primary. Claude Opus 5.5 independently reviewed the direction and proposed cleanup; see its [verbatim review](claude_direction_reset.md), [provenance](direction_reset_claude_provenance.json) and [accepted/deferred decisions](reset_decisions.md). Claude supported the reset and identified the old novelty-before-building requirement as counterproductive. Its review is advice, not evidence of novelty.

Before removal, the distinct Codex independent_review worker verified all 188 manifest paths against their original committed bytes, confirmed the exact retained-file complement, and approved the scoped removal. Git history was not rewritten. The primary verified that all 181 ignored raw-run files retained their sizes and modification times across removal.

Final cleanup review by the distinct independent_review worker approved the exact staged removals, all archived hashes, all three Claude provenance hashes and verbatim report, package import and active relative Markdown links. It confirmed 181 raw files remain; size/mtime continuity was checked separately by the primary. The distinct research_critic approved the direction and proposed Notion summary, requesting explicit action/time inputs for model predictions and a distinction between noise and unexplained model error. Both clarifications were added to the laboratory design. The primary reviewed both reviewers’ findings. At the time of that cleanup, no active test suite or new experimental result was claimed.

## First connected tool laboratory — September 26, 2026

The distinct research_critic authored and committed the public API, hidden development/challenge/noise worlds and eight boundary tests in `e6920e7`, before learner execution. Codex primary reviewed those sources and tests: persistent state, tool timing, per-tick noise draws, budget enforcement and atomic rejection are consistent. This is source separation, not formal developer blinding.

Codex primary authored the bounded model interpreter/search, investigator, harness, implementation contract and learner tests. The distinct independent_review worker caught lost coefficient revisions on timeout, incomplete byte accounting, missing post-plan/final CPU checks and an unenforced two-term limit. Those were fixed and re-reviewed. Root reviewed the worker’s findings. Claude Opus 5.5 independently reviewed the plan and then code via existing subscription access; both verbatim reviews and hashes are retained. Its weighting, failure-trigger, lag timing, fresh-evaluation and reference-baseline objections led to concrete changes; disagreements and representation limits are recorded in the implementation contract.

The distinct prior_art worker authored the self-contained replay exporter and three safety tests. Root reviewed source and tests, including safe script embedding, DOM text rendering and trace fidelity; requested clearer labels for structural prequential scores and noise assumptions were applied. Browser/result checks follow the frozen demonstration. Twenty focused tests passed before that demonstration; no test count from retired studies is included.

Final implementation amendments (evidence weighting, prediction-failure trigger, seen structures, home reference) were independently re-reviewed and approved before source freeze 92f5075. Nine predeclared runs then completed without tuning. The independent reviewer checked all source hashes and clean-run status, exact journal/event identity, every evaluation error and 3,592 training prediction errors (zero numerical discrepancy), equal 80 training/44 evaluation tool costs per run, and CPU/byte caps. No learner reruns were used for that audit. Browser local-file navigation was blocked by policy; no workaround was attempted, so playback is not browser-verified. Final compact-artifact and report review approved publication: all 30 manifest entries match raw files, all three summaries match, the tracked replay is byte-identical to the raw active-development export, and current relative links resolve. The proposed Notion/final summary was also approved.

## Observation-guided proposal implementation — September 26, 2026

Codex primary authored the guided proposer, learner/runner instrumentation, tests and design response. Claude Opus 5.5 gave a read-only design critique through existing subscription access; original report and three hashes are retained. Its full feature screen, incumbent-episode reset and decision-influence diagnostic recommendations were incorporated. Noise-term prohibition and shadow enumeration were rejected with reasons; no human-cognition or novelty claim.

A distinct research_critic approved the final design and interpretation limits. A distinct independent_review worker inspected code, chronology, shortlist-before-fitting, counters, legacy-mode preservation, timeout propagation and Claude provenance; approved freeze and six predeclared developmental comparisons. The primary reviewed those findings and added the suggested guided-path partial-timeout regression. Twenty-eight focused tests passed before the demonstration.

The distinct prior_art worker extended the offline replay. Primary reviewed its source and tests: exact saved evidence/proposal/next-action joins, safe text rendering, old-trace compatibility and explicit fixed-weight counterfactual limitations. No browser verification is claimed; local-file preview was previously blocked and no workaround was attempted.

The six runs used clean source d567435 with no tuning. Independent audit recomputed all metrics with zero numerical discrepancy, checked 336 recorded support observations against frozen predictions/versions and reconstructed public features, and recomputed 1081 remove-one-model decision diagnostics. Proposal-fit, diagnostic-screening and refit counters match traces; all runs satisfy 80 training/44 evaluation tool budgets and CPU/byte caps. Enumeration exactly reproduces the v1 action sequences, selected models and evaluator records. Guided prediction improved in development but worsened in challenge/noise; no universal improvement or hypothesis-confirmation claim. The primary reviewed and accepted these findings.

Final compact-artifact/report review approved publication: all 24 raw-file hashes and byte counts match, compact run summaries equal originals, and the tracked replay is byte-identical. The reviewer independently verified the example, proposal-origin counts, decision-influence counts and paired-error summaries, plus current relative links. The short Notion/final summary was approved with the same mixed-result and opt-in qualifications. This closes the review chain.

## Partial-association repair — September 26, 2026

The separate research_critic and primary independently diagnosed early shortlist crowding at action 11 and later evidence starvation in the saved challenge trace. The original evidence-reset suspicion was not treated as an established cause. Claude Opus 5.5 challenged a grouping draft and recommended conditioning feature associations on incumbent terms. The primary discarded its uncommitted, unexecuted grouping draft and implemented only opt-in guided-partial; evidence retention/allocation/grammar/planner remain unchanged.

The primary authored code, four focused tests, prefix diagnostic and design response. The distinct independent_review worker checked source, constant/collinear handling, old-mode preservation, accounting and the exact saved prefix. It independently reproduced partial motion scores to 1.11e-16, verified old shortlist identity, and confirmed v+u enters and is selected with no post-action 11 data. It approved the fixed six-run design and all three Claude provenance hashes/verbatim report. The primary reviewed these findings. Thirty-two tests passed before source freeze and any new world run. Known limitations include suppression of correlated replacement features, unchanged evidence starvation and representation limits; no general improvement is presumed.
