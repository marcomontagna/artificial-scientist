# Follow-up A: choosing a hypothesis versus confirming it

Pre-execution design; no new learner outcomes inspected. Existing v1 control/stress examples show internal/external reversals; horizon, state distribution and selection effects are competing explanations, not diagnosed exclusive causes. Primary research supports testing prospective confirmation and rollout horizon, not a novel algorithm claim.

## Fixed learner and four rules

Keep revision_v1 worlds, initial structure, generic operators, fitter, action menu,80training cost, thresholds and maximum2cycles. Keep the same four external sequences/68evaluation units. Each external sequence is a continuous frozen rollout from a reset, with no observed-state restart. The horizon hypothesis is alignment with this intended prediction task, not universal model validity. Compare four opt-in check rules, all active exploration outside confirmation:

- original: existing4two-action discriminative experiments (8units), then best-alternative adoption with original15%/parameter margin. This remains default.
- pooled:4discriminative experiments, followed by4predeclared coverage experiments; all candidates/coefficients remain frozen. Choose best alternative using pooled eight-experiment loss and original margin. Cost16units.
- confirm_short: same16unit schedule. Select one alternative after the first4experiments and freeze its identity. The second4coverage experiments use two-step prediction tapes reinitialized from each experiment's observed start. Adopt only if the selected alternative beats the incumbent by the original margin on BOTH screening and confirmation batches. Other recorded candidates cannot replace it using confirmation answers.
- confirm_long: same as confirm_short, but freeze one continuous8-action tape from the observed state/history after screening; do not reinitialize from intermediate observed positions. Score all eight confirmation positions. The action sequence is identical to confirm_short when screening histories match.

The coverage suffix is fixed at screening end by greedily minimizing existing experiment-index visit counts (nine discrete action pairs, not state/action bins) with deterministic ties and virtual visit increments. Its actions never depend on suffix outcomes or candidate predictions. Store the full sequence, frozen models, start state and entire long tape in the pre-action journal before the first suffix action. Confirm_short journals each newly restarted tape before its own action pair; those future observed starts are not available earlier. Log per-position errors; the original margin applies to each complete stage mean, including the long eight-position mean. Partial confirmation never authorizes adoption. Count every action within80; require16remaining units to trigger non-original checks. Cooldown6experiments/max2cycles unchanged. Even a failed screening winner completes confirmation so pooled/split budgets and schedules match; failed screening cannot be rescued into adoption by confirmation under split rules. Pooled remains free to adopt using pooled losses.

During confirmation there is no refitting. Original and pooled use mean squared2D error over their complete checks; split rules log screening/confirmation losses and margins separately. Decide before refitting with check data. Timeouts retain partial evidence/work and unresolved status. No new thresholds after outcomes.

## Prospective comparison

Three unchanged worlds × three new sensor seeds280001–280003 × four rules =36sequential runs, active policy. New noise draws on familiar laws are development evidence, not independent-world confirmation. Default-original unchanged behavior is checked against prior traces via deterministic saved-history replay before runs. No additional learner/world smoke.

Primary comparison confirm_long versus confirm_short isolates prediction restart horizon within the common confirmation schedule; differences after adoption may cause later histories to diverge and are reported. Confirm_short versus pooled changes winner selection/adoption evidence, not just the policy; do not attribute any gain to a single cause. Original comparison is resource-allocation context, not a matched-cost mechanism test.

Report per-world/seed final external loss, triggered/completed cycles, adoptions, model size, unresolved flags, check costs and frozen-snapshot external losses. Harmful accepted snapshot means its external MSE exceeds its OWN frozen incumbent on common evaluator tapes; it does not prove the final refitted agent was harmed. Useful accepted snapshot means the adopted snapshot beats its own frozen incumbent externally by the SAME original15%/parameter margin. Missed useful revision means a rejected decision candidate would beat that margin externally. Candidate means screening winner for split rules, pooled winner for pooled, and original winner for original; denominators are reported per completed cycle, per adoption and per all three runs. Track missed useful revisions using the frozen rejected decision candidate, never use this external diagnostic for adoption. Original posthoc best-alternative margin is not the full two-stage rule; label that diagnostic separately.

The predesignated confirm_long rule is encouraging versus pooled only if control and stress means are each lower, harmful accepted counts do not rise in either world and fall strictly in their combined total, at least2/3challenge runs have a useful accepted snapshot, and meanchallengeerror is no more than1.25times original+.0001. For the primary long-versus-short contrast, report the SAME conditions with confirm_short as comparator, plus harmful/useful rates per adoption and missed-useful counts. A zero-adoption denominator is undefined, not a perfect success rate. Report first-cycle histories/decisions separately from later divergent trajectories. Report each condition and paired outcomes separately; no omnibus success/significance claim. A rule that rejects everything is not an improvement. Every rule, including failures, remains recorded. No winner becomes default automatically.

## Deliverable

Freeze source/design before36runs. Claude design/result critiques with provenance, independent code/result audit, compact summaries/raw hashes and one predeclared first-seed challenge replay for confirm_long. Existing replay should explicitly distinguish a full confirmation tape from a locally restarted two-step prediction. Browser playback verification remains separate. Next experiment is selected only after results and critique; no added lag or simulator-specific operator.

## Review decisions before coding

Claude and an independent reviewer checked the design. Accepted exact journal timing, experiment-index visits, complete-batch-only adoption, per-position errors, continuous evaluator disclosure, separate control/stress criteria and count/rate denominators. Default-original replay must preserve prior recorded decisions before the new runs. The same family/thresholds and current worldsource remain fixed.

We do not adopt Claude's proposed universal fewer-than-two-disagreements cutoff or a per-adoption comparison in world/seed cells with zero adoptions: those rules confuse sparse evidence with proof that confirmation cannot help. All disagreement counts and undefined denominators will be shown. If benefits arise only by rejecting useful challenge revisions or rates do not improve, do not pursue blind gate tuning. Prior45runs used11.32CPU seconds total; this36run comparison fits the resource cap without a new pre-freeze world smoke.
