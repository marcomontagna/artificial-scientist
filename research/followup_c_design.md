# Follow-up C: unchanged investigator in independently authored worlds

Pre-execution design afterB. Freeze learner/model/proposal/action/gate sources at2b0eca1. No weight transfer: each run starts fresh; this is cross-world validation of the same learning machinery, not transfer learning. Primary question: does useful input-history construction recur when another agent writes different laws? Secondary question: does the learner acknowledge externally poor prediction?

## Before worlds are authored

Freeze this protocol and an evaluator-only sparse history-regression reference, then ask a separate worker to author two persistent worlds with the same public tool interface, noise convention,80training cap and stable finite dynamics. Coordinator will not read their numeric laws until all declared outcomes exist. A different reviewer inspects source/tests. World author sees the declared language/capability but must not inspect B outcomes or fit a learner to their world. Seal source/specification hashes in a commit before any learner run. This reduces immediate co-design; it is not formal blindness or a random population of worlds.

One world (transfer_a) is exactly representable with≤4terms in the declared runtime grammar and has a nontrivial delayed-input component. The other (transfer_b) is a stable linear input-history system with five independently relevant features, all delays within1..16, exceeding the runtime4term cap. Do not use an out-of-range lag as the challenge. The reference has capacity8terms. Magnitudes cannot make the extra components negligible, and world author/reviewer check structural independent relevance analytically, without performance tuning. Choose different coefficients, axis relationships and delays from existing known laws. This is a deliberately capability-aligned pair, not general-universe coverage.

Keep the four fixed evaluation sequences and68units unchanged. Reviewer must verify before runs that the declared extra delayed effects can occur within the16tick longest sequence; if not, revise the world specification BEFORE source sealing, not after results. Every external tape has ownreset/emptyhistory.

## Sparse reference, fixed before world choice

Use all39ordinary/inputhistory features, safe sameinterpreter, ridge1e-4 and coefficient clamp10. Greedy forward selection fromzero, up to8terms, separate coefficients peraxis. For each stage choose lowest training one-step squared-error model on the first60% of recorded training transitions (tiesbyidentifier). Score each stage0..8 on one continuous rollout over the remaining40%, initialized with only the prefix history. Choose the stage with lowest validation position-MSE (ties fewerterms thenidentifier), then refit that structure on alltrainingdata. Validation is part of training, not external evaluation. No evaluator outcomes select features or size. Log full path, validation losses, selectedsize, fit/feature costs and splitboundary. Reference is evaluator-only and never guides learner decisions. Runtime remains4terms; higherreferencecapacity/compute explicit. Keep old9feature and39feature references for transparency. This is a declared greedy ARX-style comparator, not guaranteed state-of-the-art or an implementation of lasso.

## Prospective runs and criteria

2newworlds ×3newseeds300001–300003 ×4arms =24runs: ordinary-active, history-active, history-random, history-coverage. All use original checks,80training/68eval units,120CPU-seconds, fresh raw paths and source hashes. No extra smoke or tuning after outcomes. Predeclare replay transfer_a/historyactive/300001.

On transfer_a the capability criterion is history-active mean external MSE≤.8ordinary-active and at least2/3paired wins. Report absolute errors, per-seed models, missing/spurious terms relative to the sealed law only after completion, and compare random/coverage and sparse reference on the same acquired observations. A gain over ordinary but not random supports representation, not acquisition superiority. A gain over simple reference but not sparse reference limits claims about construction. No automatic default promotion.

For transfer_b report error versus all references and whether the supplied failure detector flags it. Before outcomes define externally poor as mean external2D positionMSE>.01; this is an engineering reporting threshold, not calibrated hypothesis testing or causal truth. A detector flag means status=budget_complete AND explanation_status=unresolved. Any incomplete_check or CPU/resource stop is tabulated separately as incomplete and excluded from the detector-success numerator; other completed labels are unflagged and not claims of confidence. Tabulate all runs flagged/unflagged against poor/notpoor; display raw errors/thresholds so scale and horizon mismatch remain visible. The status-alignment criterion requires at least2/3history-active runs to be externally poor AND flagged; if fewer than2arepoor the intended failure test is uninformative, not a pass. Successful approximation despite absent exact expressibility is allowed; structural mismatch alone does not require an alarm.

Harmful/useful/missed retain own-frozen-incumbent definitions fromA/B and are separate from absolute adequacy. Each world/seed is descriptive; small designed sample cannot establish calibrated failure awareness or novelty. Freeze design, get Claude critique, implement/reference review, seal worlds, executeall24, independent numericalaudit, Claude resultcritique, cumulative shortreport. Stopnewruns by22:15UTC.

## Independent design clarifications

ForNchronological training transitions, split at floor(.6N), not flattened per-axis rows. Selection fits only the prefix. Validation starts at the boundary's observed position with prefix-only action/velocity history and predicts all suffix actions continuously; no suffix observed positions restart the rollout. The zero-term stage predicts persistence after its observed start. Feature-stage ties use model identifiers; final validation ties favor fewer terms thenidentifier. The39feature pool is exactly the same as the existing flat reference; max8terms is evaluator-only. An8term comparator against4term runtime also changes capacity, so any difference cannot isolate selection algorithm quality. Five independent features do not guarantee MSE>.01: retain the uninformative status-test outcome if the challenge is not externally poor.

## Claude design response before implementation

A term is one scalar feature expression in a common ordered feature list, evaluated for each coordinate; its coefficients may differ by axis. Runtime has≤4such feature expressions (up to8coefficients); sparse reference≤8features (up to16coefficients); the transfer_b true law needs five such independent expressions. transfer_a must include a lag≥2 that cannot be absorbed into the ordinary variables. Reviewer checks these properties analytically.

All arms within each world/seed share the same seeded sensor process; evaluator tapes/outcomes are identical across arms. Training actions can change physical observations, intentionally. For incomplete runs report the entire declared study as incomplete and do not select a convenient subset. All runs must be launched and finished before22:15UTC; stop at the deadline and report partial evidence. Both worlds get all arm/reference comparisons.

Claude's proposed boundary-aligned reset split is unnecessary here: training has no resets, and generic reference rollout applies any explicit reset action exactly, clears memory, and uses the known home position without reading validation observations. Split only between whole transitions, never within a wait/reset. A synthetic reset-in-validation test verifies this.

We do not add a pre-seal oracle-fitting loop or tune world difficulty until a best4term approximation exceeds.01. That would select the challenge using predictive outcomes and remove the declared uninformative possibility. Instead require analytically nonzero independent extra terms and observability under the fixed actions. Both worlds inherit Gaussian sensorσ=.01; an ideal evaluator knowing exact latent positions has expected2D sensor-only MSE2σ²=.0002. This analytic noise reference excludes estimation and state-initialization errors; it is not a measured oracle score or guaranteed attainable learner floor. The frozen detector sees short internal residuals, so external-horizon mismatch can cause misses; such misses measure the limitation rather than unexpected awareness.
